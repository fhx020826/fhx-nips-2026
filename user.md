# 用户偏好与长期约束

## 工作区约束

1. 所有后续操作都在 `/home/hxfeng/fhx-nips-2026` 内进行。
2. 不要阅读或影响其他项目目录。
3. 项目要保持干净整洁，旧代码、失败结果、过时文档需要定期评估是否清理。
4. 当前开发环境是：
   - 本机：Windows
   - 远端：Linux HPC 跳板机/集群
5. 不要在跳板机上直接运行重负载程序；重计算统一走 SLURM。

## 网络与代理

1. 访问外网必须优先走代理。
2. 本机代理入口不是二进制命令，而是 shell function：
   - `clash`
   - `proxy`
3. 在非交互命令中应显式使用：

```bash
bash -ic 'proxy >/dev/null; <command>'
```

4. 当前 Clash 端口：
   - HTTP: `127.0.0.1:18990`
   - SOCKS: `127.0.0.1:18991`
   - Mixed: `127.0.0.1:18993`

## 论文调研与粗读维护

1. 每次批量生成论文粗读时，都必须同步做事实核查，不允许只依据已有本地摘要或历史记忆直接写卡片。
2. 事实核查必须优先使用外网来源，包括：
   - arXiv / OpenReview / publisher / project page
   - GitHub 仓库与 README
   - 模型页、数据页、项目主页
3. 读取 GitHub 相关内容时也必须视作外网访问，统一走代理。
4. 每批粗读都要同时检查：
   - 论文基本信息是否准确
   - benchmark 可比性表述是否准确
   - 代码 / 项目页 / checkpoint 状态是否准确
   - 是否应纳入内部高质量论文列表
5. 如果论文已经在 `docs/paper_quality_shortlist.md` 中，则必须同步复核该 shortlist 条目的日期、定位、优先用途和描述是否仍然正确。
6. 对于 `sampled subset`、`pre-exploration`、`额外传感器`、`扩展 benchmark`、`非标准主榜设定` 等情况，必须在粗读与 shortlist 中显式标注，避免误导性横向比较。
7. 粗读最终成稿要保持规范化、可直接复制粘贴到飞书，但不要机械照抄模板。
8. 模板中的核心信息需要覆盖完整，但具体组织方式应根据论文内容调整，不要堆过多生硬的“是否”字段。
9. 重点部分可以分点增强可读性，但不是所有小节都必须拆成清单；应优先保证内容密度、专业性和自然性。
10. 对每篇论文都要独立完成完整调研过程，包括论文正文、arXiv / venue / project page / GitHub README / 模型页 / 数据页等外部入口，而不是仅基于已有摘要改写。

## 环境隔离

1. 所有 Python 代码与运行都使用独立 conda 环境。
2. 项目默认环境名：`fhx-nips-2026`。

## Git 与同步

1. 远程仓库：`fhx020826/fhx-nips-2026`
2. 重要代码和文档更新要同步到远程。
3. 大测试结果、中间日志、数据集、模型权重默认不上传。

## 文档要求

以下文件必须长期维护：

- `coding.md`: 统一代码规范
- `project.md`: 基于真实代码和当前研究状态的项目理解
- `user.md`: 用户偏好、常用命令、长期约束
- `work.md`: 每轮对话后的项目进展与下一步安排
- `docs/hpc_handbook.md`: HPC 使用摘要与常用命令

## 输出要求

1. 每轮对话后都要清晰说明：
   - 本轮进展
   - 下一步安排
2. 若发现可清理文件，要先告知用户，不直接删除。
